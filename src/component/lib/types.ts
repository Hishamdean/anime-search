export interface ResultCardType {
  title: string,
  image_url: string,
  rank: number,
  background: string,
}

export interface DescriptionType {
  users: number,
  ranked: number,
  popularity: number,
  members: number,
  title?: string,
  background: string,
}