export interface ResultCardType {
  title: string,
  image_url: string,
  rank: number,
  background: string,
}

export interface DescriptionType {
  synopsis: string,
  title: string,
  image_url: string,
  scored_by: number,
  rank: number,
  popularity: number,
  members: number,
  score: number,
}